</p>
<div class="row">      
<div class="row">
  <div class="hide-on-mobile">
    <%= render 'shared/sidebar' %>
  </div>
</div>

<div class="row">
  <div class="col-sm-8">
    <div class="col-sm-8">
        <% if @top.empty? && @bottom.empty? && @outfit.empty?%>
        <center><h3>You Don't Have Dirty Clothes!</h3>
        <% else %>
        <center><h1>Items in Hamper</h1>  <%= button_to 'Wash Everything', hamper_wash_all_path, class: "btn btn-success washeverything", method: :post %></center>

        <%= render 'hamper_tops'%>
        <%= render 'hamper_bottoms'%>
        <% end %>
        </center>
      </div>
</div>
</div>
</div>