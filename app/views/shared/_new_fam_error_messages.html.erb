<% if @new_fam.errors.any? %>
<div id="error_explanation">
  <div class="alert alert-danger">
    The form contains <%= pluralize(@new_fam.errors.count, "error") %>.
  </div>
  <ul>
  <% @new_fam.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>