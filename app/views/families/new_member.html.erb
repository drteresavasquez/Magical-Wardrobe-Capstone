<% provide(:title, 'Add New Family Member') %>
<% provide(:button_text, 'Create Family Member') %>

<h2>Add New Family Member</h2>

<%= render 'add_family_member_form' %>

<div class="center">
<%= link_to 'Back', root_path, class: "btn btn-info btn-lg form-btn" %></div>