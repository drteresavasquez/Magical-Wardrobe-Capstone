<% provide(:button_text, 'Update Family') %>
<h1>Editing Family</h1>

<%= render 'form', family: @family %>
<div class="col-sm-12 center">
        <%= link_to 'Back', root_path, class: "btn btn-info btn-lg form-btn" %></div>
